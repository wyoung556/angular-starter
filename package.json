{
  "name": "angular-starter",
  "version": "1.0.0",
  "description": "An Angular starter app",
  "scripts": {
    "start": "http-server -o -a localhost -d false -p 8000 -c-1 ./app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Wayland Young",
  "dependencies": {
    "angular": "^1.5.8",
    "bootstrap": "^3.3.7",
    "gulp": "^3.9.1",
    "gulp-sourcemaps": "^1.6.0",
    "http-server": "^0.9.0"
  }
}
